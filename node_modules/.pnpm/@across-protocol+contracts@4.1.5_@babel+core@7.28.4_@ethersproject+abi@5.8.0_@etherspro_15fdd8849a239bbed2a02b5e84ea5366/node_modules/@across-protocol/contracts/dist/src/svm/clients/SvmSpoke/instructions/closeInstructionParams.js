"use strict";
/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.CLOSE_INSTRUCTION_PARAMS_DISCRIMINATOR = void 0;
exports.getCloseInstructionParamsDiscriminatorBytes = getCloseInstructionParamsDiscriminatorBytes;
exports.getCloseInstructionParamsInstructionDataEncoder = getCloseInstructionParamsInstructionDataEncoder;
exports.getCloseInstructionParamsInstructionDataDecoder = getCloseInstructionParamsInstructionDataDecoder;
exports.getCloseInstructionParamsInstructionDataCodec = getCloseInstructionParamsInstructionDataCodec;
exports.getCloseInstructionParamsInstructionAsync = getCloseInstructionParamsInstructionAsync;
exports.getCloseInstructionParamsInstruction = getCloseInstructionParamsInstruction;
exports.parseCloseInstructionParamsInstruction = parseCloseInstructionParamsInstruction;
const kit_1 = require("@solana/kit");
const programs_1 = require("../programs");
const shared_1 = require("../shared");
exports.CLOSE_INSTRUCTION_PARAMS_DISCRIMINATOR = new Uint8Array([
    224, 44, 254, 10, 216, 8, 172, 96,
]);
function getCloseInstructionParamsDiscriminatorBytes() {
    return (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8).encode(exports.CLOSE_INSTRUCTION_PARAMS_DISCRIMINATOR);
}
function getCloseInstructionParamsInstructionDataEncoder() {
    return (0, kit_1.transformEncoder)((0, kit_1.getStructEncoder)([['discriminator', (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8)]]), (value) => ({
        ...value,
        discriminator: exports.CLOSE_INSTRUCTION_PARAMS_DISCRIMINATOR,
    }));
}
function getCloseInstructionParamsInstructionDataDecoder() {
    return (0, kit_1.getStructDecoder)([
        ['discriminator', (0, kit_1.fixDecoderSize)((0, kit_1.getBytesDecoder)(), 8)],
    ]);
}
function getCloseInstructionParamsInstructionDataCodec() {
    return (0, kit_1.combineCodec)(getCloseInstructionParamsInstructionDataEncoder(), getCloseInstructionParamsInstructionDataDecoder());
}
async function getCloseInstructionParamsInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? programs_1.SVM_SPOKE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        signer: { value: input.signer ?? null, isWritable: true },
        instructionParams: {
            value: input.instructionParams ?? null,
            isWritable: true,
        },
    };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.instructionParams.value) {
        accounts.instructionParams.value = await (0, kit_1.getProgramDerivedAddress)({
            programAddress,
            seeds: [
                (0, kit_1.getBytesEncoder)().encode(new Uint8Array([
                    105, 110, 115, 116, 114, 117, 99, 116, 105, 111, 110, 95, 112, 97,
                    114, 97, 109, 115,
                ])),
                (0, kit_1.getAddressEncoder)().encode((0, shared_1.expectAddress)(accounts.signer.value)),
            ],
        });
    }
    const getAccountMeta = (0, shared_1.getAccountMetaFactory)(programAddress, 'programId');
    const instruction = {
        accounts: [
            getAccountMeta(accounts.signer),
            getAccountMeta(accounts.instructionParams),
        ],
        programAddress,
        data: getCloseInstructionParamsInstructionDataEncoder().encode({}),
    };
    return instruction;
}
function getCloseInstructionParamsInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? programs_1.SVM_SPOKE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        signer: { value: input.signer ?? null, isWritable: true },
        instructionParams: {
            value: input.instructionParams ?? null,
            isWritable: true,
        },
    };
    const accounts = originalAccounts;
    const getAccountMeta = (0, shared_1.getAccountMetaFactory)(programAddress, 'programId');
    const instruction = {
        accounts: [
            getAccountMeta(accounts.signer),
            getAccountMeta(accounts.instructionParams),
        ],
        programAddress,
        data: getCloseInstructionParamsInstructionDataEncoder().encode({}),
    };
    return instruction;
}
function parseCloseInstructionParamsInstruction(instruction) {
    if (instruction.accounts.length < 2) {
        // TODO: Coded error.
        throw new Error('Not enough accounts');
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            signer: getNextAccount(),
            instructionParams: getNextAccount(),
        },
        data: getCloseInstructionParamsInstructionDataDecoder().decode(instruction.data),
    };
}
