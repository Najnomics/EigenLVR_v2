/** Mapping: chainId -> contractName -> { address, blockNumber }. */
export type Deployments = Record<string, Record<string, {
    address: string;
    blockNumber: number;
}>>;
export declare const DEPLOYMENTS: Readonly<Deployments>;
/**
 * Returns the deployed address of any contract on any network.
 */
export declare function getDeployedAddress(contractName: string, networkId: number | string, throwOnError?: boolean): string | undefined;
/**
 * Returns all active deployments for a given contract name across all chains.
 * Each result contains chainId, address, and blockNumber.
 */
export declare function getAllDeployedAddresses(contractName: string): Array<{
    chainId: number;
    address: string;
    blockNumber: number;
}>;
/**
 * Returns the deployment block number of any contract on any network.
 */
export declare function getDeployedBlockNumber(contractName: string, networkId: number): number;
/**
 * Returns the chainId and contract name for a given contract address.
 */
export declare function getContractInfoFromAddress(contractAddress: string): {
    chainId: Number;
    contractName: string;
};
