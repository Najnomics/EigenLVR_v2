"use strict";
/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SET_SIGNATURE_THRESHOLD_DISCRIMINATOR = void 0;
exports.getSetSignatureThresholdDiscriminatorBytes = getSetSignatureThresholdDiscriminatorBytes;
exports.getSetSignatureThresholdInstructionDataEncoder = getSetSignatureThresholdInstructionDataEncoder;
exports.getSetSignatureThresholdInstructionDataDecoder = getSetSignatureThresholdInstructionDataDecoder;
exports.getSetSignatureThresholdInstructionDataCodec = getSetSignatureThresholdInstructionDataCodec;
exports.getSetSignatureThresholdInstruction = getSetSignatureThresholdInstruction;
exports.parseSetSignatureThresholdInstruction = parseSetSignatureThresholdInstruction;
const kit_1 = require("@solana/kit");
const programs_1 = require("../programs");
const shared_1 = require("../shared");
exports.SET_SIGNATURE_THRESHOLD_DISCRIMINATOR = new Uint8Array([
    163, 19, 154, 168, 82, 209, 214, 219,
]);
function getSetSignatureThresholdDiscriminatorBytes() {
    return (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8).encode(exports.SET_SIGNATURE_THRESHOLD_DISCRIMINATOR);
}
function getSetSignatureThresholdInstructionDataEncoder() {
    return (0, kit_1.transformEncoder)((0, kit_1.getStructEncoder)([
        ['discriminator', (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8)],
        ['newSignatureThreshold', (0, kit_1.getU32Encoder)()],
    ]), (value) => ({
        ...value,
        discriminator: exports.SET_SIGNATURE_THRESHOLD_DISCRIMINATOR,
    }));
}
function getSetSignatureThresholdInstructionDataDecoder() {
    return (0, kit_1.getStructDecoder)([
        ['discriminator', (0, kit_1.fixDecoderSize)((0, kit_1.getBytesDecoder)(), 8)],
        ['newSignatureThreshold', (0, kit_1.getU32Decoder)()],
    ]);
}
function getSetSignatureThresholdInstructionDataCodec() {
    return (0, kit_1.combineCodec)(getSetSignatureThresholdInstructionDataEncoder(), getSetSignatureThresholdInstructionDataDecoder());
}
function getSetSignatureThresholdInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? programs_1.MESSAGE_TRANSMITTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        attesterManager: {
            value: input.attesterManager ?? null,
            isWritable: false,
        },
        messageTransmitter: {
            value: input.messageTransmitter ?? null,
            isWritable: true,
        },
        eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
        program: { value: input.program ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    const getAccountMeta = (0, shared_1.getAccountMetaFactory)(programAddress, 'programId');
    const instruction = {
        accounts: [
            getAccountMeta(accounts.attesterManager),
            getAccountMeta(accounts.messageTransmitter),
            getAccountMeta(accounts.eventAuthority),
            getAccountMeta(accounts.program),
        ],
        programAddress,
        data: getSetSignatureThresholdInstructionDataEncoder().encode(args),
    };
    return instruction;
}
function parseSetSignatureThresholdInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        // TODO: Coded error.
        throw new Error('Not enough accounts');
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            attesterManager: getNextAccount(),
            messageTransmitter: getNextAccount(),
            eventAuthority: getNextAccount(),
            program: getNextAccount(),
        },
        data: getSetSignatureThresholdInstructionDataDecoder().decode(instruction.data),
    };
}
