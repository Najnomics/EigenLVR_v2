"use strict";
/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SET_MAX_MESSAGE_BODY_SIZE_DISCRIMINATOR = void 0;
exports.getSetMaxMessageBodySizeDiscriminatorBytes = getSetMaxMessageBodySizeDiscriminatorBytes;
exports.getSetMaxMessageBodySizeInstructionDataEncoder = getSetMaxMessageBodySizeInstructionDataEncoder;
exports.getSetMaxMessageBodySizeInstructionDataDecoder = getSetMaxMessageBodySizeInstructionDataDecoder;
exports.getSetMaxMessageBodySizeInstructionDataCodec = getSetMaxMessageBodySizeInstructionDataCodec;
exports.getSetMaxMessageBodySizeInstruction = getSetMaxMessageBodySizeInstruction;
exports.parseSetMaxMessageBodySizeInstruction = parseSetMaxMessageBodySizeInstruction;
const kit_1 = require("@solana/kit");
const programs_1 = require("../programs");
const shared_1 = require("../shared");
exports.SET_MAX_MESSAGE_BODY_SIZE_DISCRIMINATOR = new Uint8Array([
    168, 178, 8, 117, 217, 167, 219, 31,
]);
function getSetMaxMessageBodySizeDiscriminatorBytes() {
    return (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8).encode(exports.SET_MAX_MESSAGE_BODY_SIZE_DISCRIMINATOR);
}
function getSetMaxMessageBodySizeInstructionDataEncoder() {
    return (0, kit_1.transformEncoder)((0, kit_1.getStructEncoder)([
        ['discriminator', (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8)],
        ['newMaxMessageBodySize', (0, kit_1.getU64Encoder)()],
    ]), (value) => ({
        ...value,
        discriminator: exports.SET_MAX_MESSAGE_BODY_SIZE_DISCRIMINATOR,
    }));
}
function getSetMaxMessageBodySizeInstructionDataDecoder() {
    return (0, kit_1.getStructDecoder)([
        ['discriminator', (0, kit_1.fixDecoderSize)((0, kit_1.getBytesDecoder)(), 8)],
        ['newMaxMessageBodySize', (0, kit_1.getU64Decoder)()],
    ]);
}
function getSetMaxMessageBodySizeInstructionDataCodec() {
    return (0, kit_1.combineCodec)(getSetMaxMessageBodySizeInstructionDataEncoder(), getSetMaxMessageBodySizeInstructionDataDecoder());
}
function getSetMaxMessageBodySizeInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? programs_1.MESSAGE_TRANSMITTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        owner: { value: input.owner ?? null, isWritable: false },
        messageTransmitter: {
            value: input.messageTransmitter ?? null,
            isWritable: true,
        },
        eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
        program: { value: input.program ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    const getAccountMeta = (0, shared_1.getAccountMetaFactory)(programAddress, 'programId');
    const instruction = {
        accounts: [
            getAccountMeta(accounts.owner),
            getAccountMeta(accounts.messageTransmitter),
            getAccountMeta(accounts.eventAuthority),
            getAccountMeta(accounts.program),
        ],
        programAddress,
        data: getSetMaxMessageBodySizeInstructionDataEncoder().encode(args),
    };
    return instruction;
}
function parseSetMaxMessageBodySizeInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        // TODO: Coded error.
        throw new Error('Not enough accounts');
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            owner: getNextAccount(),
            messageTransmitter: getNextAccount(),
            eventAuthority: getNextAccount(),
            program: getNextAccount(),
        },
        data: getSetMaxMessageBodySizeInstructionDataDecoder().decode(instruction.data),
    };
}
