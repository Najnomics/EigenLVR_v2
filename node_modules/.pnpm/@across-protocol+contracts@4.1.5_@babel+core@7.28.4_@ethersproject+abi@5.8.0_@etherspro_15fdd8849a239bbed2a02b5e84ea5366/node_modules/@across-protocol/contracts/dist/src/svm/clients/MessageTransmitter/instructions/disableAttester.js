"use strict";
/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.DISABLE_ATTESTER_DISCRIMINATOR = void 0;
exports.getDisableAttesterDiscriminatorBytes = getDisableAttesterDiscriminatorBytes;
exports.getDisableAttesterInstructionDataEncoder = getDisableAttesterInstructionDataEncoder;
exports.getDisableAttesterInstructionDataDecoder = getDisableAttesterInstructionDataDecoder;
exports.getDisableAttesterInstructionDataCodec = getDisableAttesterInstructionDataCodec;
exports.getDisableAttesterInstruction = getDisableAttesterInstruction;
exports.parseDisableAttesterInstruction = parseDisableAttesterInstruction;
const kit_1 = require("@solana/kit");
const programs_1 = require("../programs");
const shared_1 = require("../shared");
exports.DISABLE_ATTESTER_DISCRIMINATOR = new Uint8Array([
    61, 171, 131, 95, 172, 15, 227, 229,
]);
function getDisableAttesterDiscriminatorBytes() {
    return (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8).encode(exports.DISABLE_ATTESTER_DISCRIMINATOR);
}
function getDisableAttesterInstructionDataEncoder() {
    return (0, kit_1.transformEncoder)((0, kit_1.getStructEncoder)([
        ['discriminator', (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8)],
        ['attester', (0, kit_1.getAddressEncoder)()],
    ]), (value) => ({ ...value, discriminator: exports.DISABLE_ATTESTER_DISCRIMINATOR }));
}
function getDisableAttesterInstructionDataDecoder() {
    return (0, kit_1.getStructDecoder)([
        ['discriminator', (0, kit_1.fixDecoderSize)((0, kit_1.getBytesDecoder)(), 8)],
        ['attester', (0, kit_1.getAddressDecoder)()],
    ]);
}
function getDisableAttesterInstructionDataCodec() {
    return (0, kit_1.combineCodec)(getDisableAttesterInstructionDataEncoder(), getDisableAttesterInstructionDataDecoder());
}
function getDisableAttesterInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? programs_1.MESSAGE_TRANSMITTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        payer: { value: input.payer ?? null, isWritable: true },
        attesterManager: {
            value: input.attesterManager ?? null,
            isWritable: false,
        },
        messageTransmitter: {
            value: input.messageTransmitter ?? null,
            isWritable: true,
        },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
        eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
        program: { value: input.program ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111';
    }
    const getAccountMeta = (0, shared_1.getAccountMetaFactory)(programAddress, 'programId');
    const instruction = {
        accounts: [
            getAccountMeta(accounts.payer),
            getAccountMeta(accounts.attesterManager),
            getAccountMeta(accounts.messageTransmitter),
            getAccountMeta(accounts.systemProgram),
            getAccountMeta(accounts.eventAuthority),
            getAccountMeta(accounts.program),
        ],
        programAddress,
        data: getDisableAttesterInstructionDataEncoder().encode(args),
    };
    return instruction;
}
function parseDisableAttesterInstruction(instruction) {
    if (instruction.accounts.length < 6) {
        // TODO: Coded error.
        throw new Error('Not enough accounts');
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            payer: getNextAccount(),
            attesterManager: getNextAccount(),
            messageTransmitter: getNextAccount(),
            systemProgram: getNextAccount(),
            eventAuthority: getNextAccount(),
            program: getNextAccount(),
        },
        data: getDisableAttesterInstructionDataDecoder().decode(instruction.data),
    };
}
