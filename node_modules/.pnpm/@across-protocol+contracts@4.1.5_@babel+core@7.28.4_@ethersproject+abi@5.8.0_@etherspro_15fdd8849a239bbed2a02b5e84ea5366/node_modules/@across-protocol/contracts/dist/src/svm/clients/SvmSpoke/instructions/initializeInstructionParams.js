"use strict";
/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.INITIALIZE_INSTRUCTION_PARAMS_DISCRIMINATOR = void 0;
exports.getInitializeInstructionParamsDiscriminatorBytes = getInitializeInstructionParamsDiscriminatorBytes;
exports.getInitializeInstructionParamsInstructionDataEncoder = getInitializeInstructionParamsInstructionDataEncoder;
exports.getInitializeInstructionParamsInstructionDataDecoder = getInitializeInstructionParamsInstructionDataDecoder;
exports.getInitializeInstructionParamsInstructionDataCodec = getInitializeInstructionParamsInstructionDataCodec;
exports.getInitializeInstructionParamsInstructionAsync = getInitializeInstructionParamsInstructionAsync;
exports.getInitializeInstructionParamsInstruction = getInitializeInstructionParamsInstruction;
exports.parseInitializeInstructionParamsInstruction = parseInitializeInstructionParamsInstruction;
const kit_1 = require("@solana/kit");
const programs_1 = require("../programs");
const shared_1 = require("../shared");
exports.INITIALIZE_INSTRUCTION_PARAMS_DISCRIMINATOR = new Uint8Array([
    94, 206, 190, 192, 127, 8, 186, 28,
]);
function getInitializeInstructionParamsDiscriminatorBytes() {
    return (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8).encode(exports.INITIALIZE_INSTRUCTION_PARAMS_DISCRIMINATOR);
}
function getInitializeInstructionParamsInstructionDataEncoder() {
    return (0, kit_1.transformEncoder)((0, kit_1.getStructEncoder)([
        ['discriminator', (0, kit_1.fixEncoderSize)((0, kit_1.getBytesEncoder)(), 8)],
        ['totalSize', (0, kit_1.getU32Encoder)()],
    ]), (value) => ({
        ...value,
        discriminator: exports.INITIALIZE_INSTRUCTION_PARAMS_DISCRIMINATOR,
    }));
}
function getInitializeInstructionParamsInstructionDataDecoder() {
    return (0, kit_1.getStructDecoder)([
        ['discriminator', (0, kit_1.fixDecoderSize)((0, kit_1.getBytesDecoder)(), 8)],
        ['totalSize', (0, kit_1.getU32Decoder)()],
    ]);
}
function getInitializeInstructionParamsInstructionDataCodec() {
    return (0, kit_1.combineCodec)(getInitializeInstructionParamsInstructionDataEncoder(), getInitializeInstructionParamsInstructionDataDecoder());
}
async function getInitializeInstructionParamsInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? programs_1.SVM_SPOKE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        signer: { value: input.signer ?? null, isWritable: true },
        instructionParams: {
            value: input.instructionParams ?? null,
            isWritable: true,
        },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.instructionParams.value) {
        accounts.instructionParams.value = await (0, kit_1.getProgramDerivedAddress)({
            programAddress,
            seeds: [
                (0, kit_1.getBytesEncoder)().encode(new Uint8Array([
                    105, 110, 115, 116, 114, 117, 99, 116, 105, 111, 110, 95, 112, 97,
                    114, 97, 109, 115,
                ])),
                (0, kit_1.getAddressEncoder)().encode((0, shared_1.expectAddress)(accounts.signer.value)),
            ],
        });
    }
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111';
    }
    const getAccountMeta = (0, shared_1.getAccountMetaFactory)(programAddress, 'programId');
    const instruction = {
        accounts: [
            getAccountMeta(accounts.signer),
            getAccountMeta(accounts.instructionParams),
            getAccountMeta(accounts.systemProgram),
        ],
        programAddress,
        data: getInitializeInstructionParamsInstructionDataEncoder().encode(args),
    };
    return instruction;
}
function getInitializeInstructionParamsInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? programs_1.SVM_SPOKE_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        signer: { value: input.signer ?? null, isWritable: true },
        instructionParams: {
            value: input.instructionParams ?? null,
            isWritable: true,
        },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            '11111111111111111111111111111111';
    }
    const getAccountMeta = (0, shared_1.getAccountMetaFactory)(programAddress, 'programId');
    const instruction = {
        accounts: [
            getAccountMeta(accounts.signer),
            getAccountMeta(accounts.instructionParams),
            getAccountMeta(accounts.systemProgram),
        ],
        programAddress,
        data: getInitializeInstructionParamsInstructionDataEncoder().encode(args),
    };
    return instruction;
}
function parseInitializeInstructionParamsInstruction(instruction) {
    if (instruction.accounts.length < 3) {
        // TODO: Coded error.
        throw new Error('Not enough accounts');
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            signer: getNextAccount(),
            instructionParams: getNextAccount(),
            systemProgram: getNextAccount(),
        },
        data: getInitializeInstructionParamsInstructionDataDecoder().decode(instruction.data),
    };
}
