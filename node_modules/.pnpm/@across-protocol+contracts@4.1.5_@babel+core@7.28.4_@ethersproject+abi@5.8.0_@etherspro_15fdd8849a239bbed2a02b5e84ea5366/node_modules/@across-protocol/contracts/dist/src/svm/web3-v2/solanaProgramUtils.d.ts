import { Idl } from "@coral-xyz/anchor";
import web3, { Address, Commitment, GetSignaturesForAddressApi, Signature } from "@solana/kit";
import { RpcClient } from "./types";
type GetSignaturesForAddressConfig = Parameters<GetSignaturesForAddressApi["getSignaturesForAddress"]>[1];
/**
 * Reads all events for a specific program.
 */
export declare function readProgramEvents(rpc: RpcClient, program: Address, anchorIdl: Idl, finality?: Commitment, options?: GetSignaturesForAddressConfig): Promise<{
    confirmationStatus: string;
    blockTime: number | web3.UnixTimestamp;
    signature: web3.Signature;
    slot: bigint;
    name: string;
    program: Address;
    data: any;
}[]>;
/**
 * Reads events from a transaction.
 */
export declare function readEvents(client: RpcClient, txSignature: Signature, programId: Address, programIdl: Idl, commitment?: Commitment): Promise<{
    program: Address;
    data: any;
    name: string | undefined;
}[]>;
/**
 * For a given fillStatusPDa & associated spokePool ProgramID, return the fill event.
 */
export declare function readFillEventFromFillStatusPda(client: RpcClient, fillStatusPda: Address, programId: Address, programIdl: Idl): Promise<{
    event: any;
    slot: number;
}>;
export {};
