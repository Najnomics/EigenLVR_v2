import { FactoryOptions } from '@nomiclabs/hardhat-ethers/types';
import { BaseContract, ContractFactory, ethers } from 'ethers';
import { EDRProvider, FakeContract, FakeContractOptions, FakeContractSpec, MockContractFactory } from './types';
export declare class Sandbox {
    private vm;
    private static nonce;
    private addressToSighashToFunction;
    constructor(provider: EDRProvider);
    private overrideCall;
    private getCalledFunction;
    private addFunctionToMap;
    fake<Type extends BaseContract>(spec: FakeContractSpec, opts?: FakeContractOptions): Promise<FakeContract<Type>>;
    mock<T extends ContractFactory>(contractName: string, signerOrOptions?: ethers.Signer | FactoryOptions): Promise<MockContractFactory<T>>;
    static create(): Promise<Sandbox>;
    static getNextNonce(): number;
}
